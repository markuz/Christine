themedir = $(pkgdatadir)/icons/hicolor
size = scalable
context = actions

iconsdir = $(themedir)/$(size)/$(context)

icons_DATA = \
        repeat.svg \
		sort.svg \
		urepeat.svg \
		usort.svg 

EXTRA_DIST = \
        $(icons_DATA)
